name: zema_firestore
description: "Zema schema validation integration for Cloud Firestore. Type-safe document parsing via withConverter."
version: 0.1.0
homepage: https://zema.meragix.dev/plugins/zema_firestore
repository: https://github.com/meragix/zema
issue_tracker: https://github.com/meragix/zema/issues

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.18.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  zema: ^0.5.0
  cloud_firestore: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_cloud_firestore: ^3.0.0

topics:
  - firestore
  - firebase
  - validation
  - schema
  - zema
