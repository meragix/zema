name: zema_hive
description: "Zema schema validation integration for Hive. Type-safe local storage without TypeAdapters or code generation."
version: 0.1.0
homepage: https://zema.meragix.dev/plugins/zema_hive
repository: https://github.com/meragix/zema
issue_tracker: https://github.com/meragix/zema/issues

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  zema: ^0.5.1
  hive_ce: ^2.19.3

dev_dependencies:
  test: ^1.24.0
  lints: ^6.1.0

topics:
  - hive
  - local-storage
  - validation
  - schema
  - zema
