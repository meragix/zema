---
title: Installation
description: Requirements and setup instructions for the Zema package in Dart and Flutter projects.
seo:
  title: Installation | Zema Docs
  description: Add Zema to a Dart or Flutter project. Minimum SDK version and setup verification.
---

## Requirements

- Dart SDK **3.5.0** or higher.

---

## Add the Dependency

```yaml title="pubspec.yaml"
dependencies:
  zema: ^1.0.0
```

Install:

```bash
# Dart
dart pub get

# Flutter
flutter pub get
```

---

## Verify the Installation

```dart title="verify_zema.dart"
import 'package:zema/zema.dart';

void main() {
  final schema = z.object({
    'name': z.string().min(2),
    'age': z.integer().gte(0),
  });

  final result = schema.safeParse({
    'name': 'Alice',
    'age': 30,
  });

  if (result.isSuccess) {
    print('Installation verified. Value: ${result.value}');
  }
}
```

```bash
dart run verify_zema.dart
# Installation verified. Value: {name: Alice, age: 30}
```

---

## Import

All public types are available from a single import:

```dart
import 'package:zema/zema.dart';
```

This exposes:

- `z` and `zema`: the schema factory singleton.
- `ZemaSchema`, `ZemaResult`, `ZemaSuccess`, `ZemaFailure`: core types.
- `ZemaIssue`, `ZemaException`: error types.
- `ZemaErrorMap`, `ZemaI18n`: error customisation and localisation.

---

## Next Steps

- [Quick Start](./quick-start)
- [Core Concepts](./core-concepts)
